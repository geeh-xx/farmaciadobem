<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :medicamento_id %><br>
    <%= f.text_field :medicamento_id %>
  </div>
  <div class="field">
    <%= f.label :carrinho_id %><br>
    <%= f.text_field :carrinho_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
