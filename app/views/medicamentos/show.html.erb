<p>
  <strong>Medicamento:</strong>
  <%= @medicamento.nome %>
</p>

<p>
  <strong>Validade:</strong>
  <%= @medicamento.validade %>
</p>
<%= image_tag @medicamento.avatar.url(:medium) %><br/>

<%= link_to 'Voltar', home_index_path %><br>

<% if User.find(current_user).admin? %>
    <%= link_to 'Editar', edit_medicamento_path(@medicamento) %>
<% end %>